# Naming — closed: **GoDo** / `godo`

Product: **GoDo**. Identifiers: lowercase `godo` (binary, module, `godo.yaml`).

Repo scripts and runs (command catalog). The catalog is the product; how a host shell runs a line is separate.

Discard history: `runners`, `recipes`, `taskfiles`, `enact`, `roster`, `act` (collision with nektos/act).
